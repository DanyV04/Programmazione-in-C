#include <stdio.h> // librerie --> pezzi di codice già scritti
#include <math.h>
/*  CALCOLATRICE USO SINGOLO

int main(){
    int a;
    int b;
    int sum, prodotto, divisione, sottrazione, resto;
        printf("Inserisci un numero\n");
        scanf("%d", &a);
        printf("Inserisci un numero\n");
        scanf("%d", &b);
            sum = a + b;
        printf("Risultato\n%d", sum);  // o anche come printf("%d + %d = %d", a, b, sum);
        prodotto = a*b;
        printf("%d", prodotto);
        divisione = a/b; //con float e double --> almeno uno dei numeri deve essere decimale per non avere risultato intero
        printf("Divisione\n%d", divisione);
        resto = a%b;
        printf("Divisione con il resto\n %d", resto)
        sottrazione = a-b; 
        printf("%d", sottrazione);
        return 0;
}
*/

/*
int main(){ //di lato D
    float a , b, d;     //dichiarazione delle variabili sempre all'inizio
    float aquadrato , acerchio, atriangolo;
    printf("Inserisici un lato\n");
    scanf("%f", &d);
    aquadrato = pow(d , 2)*pi; // d*d
    printf("Area del quadrato lato D\n%f", aquadrato);
    acerchio = pow(d / 2, 2);
    printf("Area del cerchio di raggio D\n%f", acerchio);
    atriangolo = pow(d, 2)*(sqrt(3)/2);
    printf("Area del triangolo di lato D\n%f", atriangolo);
    return 0;
}
*/
/*
int main(){ //verificare quale è maggiore tra due numeri inseriti
    float a , b;
    scanf("%f", &a);
    scanf("%f", &b);
        if(a>b){
            printf("");
        }
        else {
            printf("");
        }
}
*/

//Scrivere un programma che legga un intero n e stampi se n è pari o dispari 
/*
int main(){
    int a,b;
    printf("Inserisci un numero\n");
    scanf("%d", &a);
    scanf("%d", &b);
    if (%n/2 == 0);
    {
        printf("Il numero è pari\n");
    }
    else{
        printf("Il numero è dispari\n");
    }

}
*/
